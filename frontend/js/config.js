/**
 * @file js/config.js
 * @description Configuración centralizada del frontend.
 *
 * Cambiar API_BASE aquí afecta a toda la aplicación (api.js y login.js).
 * Para desarrollo local usar: http://localhost:3000/api
 * Para producción usar la URL de Render (u otro host).
 *
 * Este archivo debe cargarse PRIMERO en cada página HTML, antes que api.js.
 */

/** URL base de la API del backend. */
const API_BASE = "https://pocketpal-6ydq.onrender.com/api";
